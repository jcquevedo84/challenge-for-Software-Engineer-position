const request = require('supertest');
const app = require('../../../app');
const errors = require('../../../util/errors.utils');

module.exports = headers =>

  describe('User suite', () => {

    /**
     * endpoint: 
     */
    it('test 1"', async (done) => {
      /*request(app)
        .post('/create-account-and-register-device')
        .then(response => {
          expect(response.statusCode).toBe(400);
          expect(response.body).toMatchObject({message: 'Missing "app.clientId" body parameter'});
          done();
        });*/
    });
  });
